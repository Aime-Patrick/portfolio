# Client Firebase — single base64 of your web firebaseConfig JSON
# Example JSON:
# {"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"...","measurementId":"..."}
# Encode:  [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes((Get-Content config.json -Raw)))
# Or:      node -e "console.log(Buffer.from(JSON.stringify(require('./firebase-web.json'))).toString('base64'))"
NEXT_PUBLIC_FIREBASE_CONFIG_BASE64=

NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=

NEXT_PUBLIC_EMAILJS_SERVICE_ID=
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=

# Server-only
NVIDIA_API_KEY=
NVIDIA_MODEL=meta/llama-3.1-8b-instruct
FIRECRAWL_API_KEY=
# Optional self-hosted Firecrawl base URL
# FIRECRAWL_API_URL=

# Firebase Admin — single base64 of the service account JSON
# (project_id, client_email, private_key)
FIREBASE_ADMIN_CONFIG_BASE64=
