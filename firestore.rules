rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Function to check if user is admin
    function isAdmin() {
      return request.auth != null && 
        (request.auth.uid == 'YOUR_ADMIN_UID' || request.auth.token.admin == true);
    }
    
    // Allow public read access to settings and profile collections
    match /settings/{document} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    match /profile/{document} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    match /projects/{document} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    match /services/{document} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    // Messages require authentication for both read and write
    match /messages/{document} {
      allow read: if isAdmin();
      allow write: if request.auth != null; // Allow authenticated users to send messages
    }
    
    // Default rule - deny all other access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}